[project]
name = "LLVM"
version = "23.0.0"
requires-python = ">=3.8"

[tool.black]
extend-exclude = '''
(
  third-party/
)
'''

[tool.pyright]
executionEnvironments = [
  { root = "lldb/packages/Python", pythonVersion = "3.8" }
]

[dependency-groups]
dev = [
    "psutil>=7.2.2",
]
